# MusicAI

곡을 검색해 가사·번역·해석·곡 설명을 보고, 파트별 난이도와 아티스트 입문 가이드까지
받아보는 개인용 웹앱. 한국어 사용자 기준으로 만든다.

## 구조

- `index.html` — 앱 전체(HTML·CSS·JS 한 파일). GitHub Pages로 서비스.
  https://kjseong0313.github.io/MusicAI/
- `src/index.js` — Cloudflare Worker. 검색 프록시와 Gemini 프록시를 겸한다.
  https://musicai.kjseong0313.workers.dev
- Gemini API 키는 Worker 환경변수 `GEMINI_KEY1` / `GEMINI_KEY2`에 있다.
  브라우저에는 키가 없다. 예전에 있던 클라이언트 키 관리 코드는 제거했다.

### 배포

```
npx --yes wrangler deploy        # 워커 (src/index.js)
git push origin main             # 프론트엔드 (index.html) → GitHub Pages
```

Pages 반영에 1~2분 걸린다. 확인하려면:
```
until curl -s "https://kjseong0313.github.io/MusicAI/index.html?cb=$(date +%s%N)" | grep -q "<찾을 문자열>"; do sleep 15; done
```

`wrangler.jsonc`에 `placement: smart`가 켜져 있다. 끄면 안 된다 — 아래 참고.

## 검색이 이렇게 생긴 이유

세 소스를 쓰는데, 각각 다른 방식으로 망가져 있어서 조합할 수밖에 없었다.
**추측하지 말고 실제로 호출해 보고 판단할 것.** 이 프로젝트의 버그는 대부분 코드를
읽어서가 아니라 상류 API를 직접 불러 봐서 나왔다.

### iTunes — Worker에서 영구 차단

Apple이 Cloudflare 대역을 429로 막는다. UA·국가·엔드포인트를 7가지로 바꿔 5회씩
시도해도 전부 429였고, 실행 위치를 도쿄→홍콩으로 옮겨도 egress 대역이 같아 그대로였다.
**이 방향은 이미 막다른 길로 확인됐으니 다시 파지 말 것.**
다만 iTunes는 `Access-Control-Allow-Origin: *`을 보내므로 **브라우저 직접 호출은
정상**이고, PC에서는 이 경로로 최상급 결과가 나온다. 모바일에서는 안 되는데 원인은
아직 확인 못 했다(통신사 공유 IP 추정).

### Deezer — 인기도의 유일한 출처, 대신 불안정

`rank`(트랙 재생수), `nb_fan`(가수 팬 수), 대표곡 목록을 주는 유일한 소스라 순위
판정의 핵심이다. 문제는 게이트웨이가 `{"data":[],"total":N}` — HTTP 200에 오류도
없이 빈 배열 — 을 자주 준다는 것.

- **위치를 타는 문제였다.** 도쿄(NRT)에서는 절반 가까이 실패했고 홍콩(HKG)에서는
  10/10 성공했다. `placement: smart`가 이걸 잡아 준다.
- 같은 URL을 다시 부르면 같은 빈 응답이 온다. 그래서 재시도마다 파라미터 모양과
  nonce(`_n=`)를 바꾸고, 간격을 점점 넓힌다(몰아치면 나쁜 구간을 통째로 맞는다).
- `Accept-Language`로 카탈로그가 바뀐다. `ko-KR`을 쓰면 한국 카탈로그가 오는데
  Ed Sheeran·IU·BTS가 통째로 빠져 있어 쓸 수 없다. **`en-US` 고정.**
- **한국 데이터는 사실상 비어 있다.** 버스커 버스커 팬 2명, 임재범 21명, 아이유는
  그 이름으로 검색조차 안 된다. 그래서 한글 검색어에는 Deezer 유명세를 아예 쓰지
  않는다(쓰면 원곡이 무명 커버에 밀리거나 필터에 잘려 나간다).

### MusicBrainz — 카탈로그는 완전, 인기도는 0

제목이 같은 커버 수십 개에 전부 `score: 100`을 준다. 그래서 자체 정렬은 무의미하다.
- 흔한 제목일수록 원곡이 뒤로 밀린다. "billie jean"은 15만 건이 매칭되고 마이클
  잭슨의 원곡은 **101번째부터** 나온다 → 두 페이지(200건)를 받는다.
- 한글은 붙여쓰기/띄어쓰기가 갈린다. "벚꽃 엔딩"으로 등록된 곡은 "벚꽃엔딩"으로
  검색하면 0건 → 띄어쓰기 넣은 형태를 OR로 함께 던진다.
- 퍼지 검색(`~`)은 라틴 문자에만 붙인다. 한글에 걸면 "소주한잔"이 "커피한잔"에 매칭된다.
- 초당 1회 제한이라 503이 잦다. 재시도가 들어 있다.

### 순위를 정하는 방식

관련도(오타·단어 생략 허용) + 인기도로 점수를 매기고, 무명 가수의 커버를 걷어낸다.
- 인기도 기준은 **절대값이 아니라 상대값**이다. 그 검색에서 가장 유명한 가수의 1%를
  선으로 삼는다. 절대값(5만)으로 자르면 한국 원곡이 통째로 사라진다.
- MusicBrainz 결과의 유명세는 Deezer에서 가수 팬 수를 빌려와 매긴다. 곡별 인기도가
  없어서 "bad guy"가 Billie Eilish(924만)가 아니라 Eminem(1,909만)에게 가는데,
  Deezer의 **가수 대표곡 50곡**에 그 제목이 있는지를 보고 가른다.
- 같은 곡이 여러 앨범에 있으면 리이슈 표시어(deluxe·edition·live 등)와 곡 제목의
  판본 표시(inst·remix 등)를 감점해 원본 앨범을 대표로 고른다.

## AI 기능

모두 Worker의 Gemini 프록시를 거친다. `/stream`은 SSE를 그대로 흘려보낸다.

- **분석 탭**: 가사 / 번역 / 해석 / 곡 설명
- **대화 탭**: 파트별 난이도 / 입문 가이드 / 표현 풀이 + 이어서 질문

지켜야 할 원칙:
- **모델 목록은 Google에서 받아온다**(`/models`). 하드코딩하지 말 것.
- 무료 한도는 API로 못 가져온다. `FREE_QUOTA`에 AI Studio 화면 값을 옮겨 적고
  확인 날짜를 함께 표시한다. **Lite 계열만 하루 500회, 나머지는 20회**라 기본
  모델은 Lite다.
- 추론 예산을 작업별로 지정한다. 3.5 Flash는 두 문장 답에도 생각 토큰을 9천 개
  넘게 쓴다. `thinkingBudget: 0`은 빈 응답을 내므로 쓰면 안 된다.
- 응답 파싱은 `thought`가 아닌 파트를 전부 이어 붙인다. `parts[0]`만 읽으면 잘린다.
- 분당 한도를 넘기지 않게 앱이 스스로 호출 간격을 벌린다(`paceCall`).
- 곡 설명·난이도는 **모르면 모른다고 쓰게** 한다. 무명 곡에 "음악사적 의의"를
  갖다 붙이지 않고, 커버 곡이면 원곡자를 먼저 밝힌다. 다만 **조성과 보컬 음역은
  반드시 적게** 한다(확실하지 않으면 추정이라고 표시). 예전에 "확실할 때만"으로
  뒀더니 항상 생략해서 정작 알고 싶은 값이 안 나왔다.

## 작업할 때 주의

- **서브요청 예산**: Workers는 요청당 50개가 상한이다. 검색 한 번의 최악이 45회라
  여유가 별로 없다. 재시도 횟수나 보강 조회 인원을 늘릴 때는 다시 계산할 것.
- **캐시**: `/itunes` 등은 URL 단위로 30분 캐싱한다. 테스트할 때 같은 URL을 쓰면
  옛 결과가 나온다. `limit` 값을 바꿔 회피할 것.
- **검증은 `limit=50`으로**. 프론트엔드가 보내는 값이고, 다른 값을 쓰면 후보 풀이
  달라져 엉뚱한 결론이 나온다.
- **한 번 돌려 보고 판단하지 말 것.** 상류가 비결정적이라 같은 질의도 회차마다
  다르다. 최소 두 번은 돌려 볼 것.
- `esc()`를 거치지 않은 값을 innerHTML이나 속성에 넣지 말 것. API가 주는 문자열도
  마찬가지다(Deezer에는 사용자 업로드 곡이 있다).
- localStorage는 `lsGet`/`lsSet`/`lsDel` 래퍼로만 접근할 것. 직접 부르면 사파리
  시크릿 모드에서 스크립트가 통째로 죽는다.

## 남은 문제

- 모바일에서 직접 iTunes 호출이 안 되는 원인 미확인. 검색 결과 위 "검색 출처" 옆
  괄호에 실패 사유가 찍히게 해뒀다.
- Deezer가 실패해 MusicBrainz로 넘어가면 흔한 제목의 곡에서 커버가 1위로 올 수 있다.
- 20곡 회귀 테스트 기준 19/20. 남는 하나는 회차마다 바뀐다.
